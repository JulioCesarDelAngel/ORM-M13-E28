/**
 * esta implementacion implicaria eliminar api de las routes/api/index.js
 * sinembargo proporciona una flexibilidad
 */
/*
const router = require('express').Router();
const apiRoutes = require('./api');  //carga el index de la ruta

router.use('/api', apiRoutes);

module.exports = router;
*/